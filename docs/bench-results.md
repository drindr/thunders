# Bench Results (legacy pointer)

The canonical workflow and the current standard 12-run matrix live in
[`bench-stability.md`](bench-stability.md) — that file is the single source
of truth and contains the table and the metric definitions.

This page is kept only as a stable path for older links. Raw captures are in
`bench/logs/*.{central,peripheral}.log`; the history of how the current ARQ
and acquisition design was reached is in
[`debugging-notes.md`](debugging-notes.md).
